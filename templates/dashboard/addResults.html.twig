<html>
    <head>
     <meta charset="UTF-8">
     <link rel="stylesheet" href="/css/newCascadeStyleSheet.css">   
    </head>
    <body>
              
        {% if validationErrors is defined %}
    {% for item in validationErrors %}
        <p>{{ item }}</p>
    {% endfor %}
    {% endif %}
        
        {#<?php
       
        $_SESSION["nrMatch"] = Db::getNrMatch()[0][0] +1;
        ?>#}
        <div class ="header">
            <nav>
                <ul>
                    <li><a href ="AddResults.php">Dodaj Wyniki</a></li>
                    <li><a href ="Ranking.php">Ranking</a></li>
                    <li><a href ="">Nagrody</a></li>
                    <li><a href ="{{ url('logout') }}">Wyloguj się</a></li>
                </ul>
            </nav>
        </div>
        
        <form method ="POST">
            {% if match != null %}
                <p>Dodajesz Wyniki do meczu nr<input type ="text" value ="{{ match.matchNr }}" name ="matchNr"></p>  
            {% else %}
                <p>Dodajesz Wyniki do meczu nr<input type ="text" value ="{{ lastMatch.matchNr + 1}}" name ="matchNr"></p>  
            {% endif %}
            <select name ="user">
                {% for user in users%}
                    <option value ={{ user.login }}>{{ user.login }} </option>
                {% endfor %}                
            </select>
            <p>Punkty<input  type="text" name ="points"></p>
            <p>Piwa<input  type="text" name ="beers" ></p>
            <p>Żetony<input type ="text" name ="tokens"></p></p>
            <p><input type ="submit" value="Zapisz">            
        </form>
        {% if resultForUserAdded != null %}
        {{ resultForUserAdded }}
        {% endif %}
    </body>
</html>
